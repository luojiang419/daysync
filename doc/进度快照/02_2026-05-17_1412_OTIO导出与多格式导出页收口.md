# DaySync 进度快照

- 时间：2026-05-17 14:12（Asia/Shanghai）
- 当前分支：`main`
- 备份状态：
  - 备份提交：`c48bf35` `backup: pre-change snapshot 2026-05-17 14:05:27`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-140527`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/2026-05-17_1405_JSON导出与导出记录收口.md`
- 上一轮结束点：
  - `CSV / FCP 7 XML / JSON` 已接入导出页
  - 下一步建议优先沿导出链路推进 `OTIO` 或 `FCPXML`

## 本次完成内容

1. 新增 `OTIO` 导出能力：
   - 后端新增 `/api/projects/{project_id}/exports/otio`
   - `daysync_core.export` 新增 `export_sync_report_otio`
   - 输出文件扩展名使用 `.otio`
2. `OTIO` 结构按官方文档的最小可用树形模型实现：
   - 顶层 `Timeline.1`
   - `tracks` 使用 `Stack.1`
   - 内含 `Video` / `Audio` 两条 `Track.1`
   - `Clip.2` 使用 `ExternalReference.1` 引用原媒体文件
   - 多条 sync result 以顺序片段方式排入同一个 timeline
3. 导出页一起收口：
   - 页头从“CSV 导出”改成“多格式导出”
   - 新增“导出 OTIO”按钮
   - 最近导出记录的计数文案按格式区分
     - `CSV` 显示“行”
     - `FCP 7 XML / FCPXML` 显示“sequence”
     - `JSON / OTIO` 显示“同步结果”

## 这次改动的关键文件

- `daysync/packages/daysync_core/src/daysync_core/export/service.py`
- `daysync/packages/daysync_core/src/daysync_core/export/__init__.py`
- `daysync/services/api/routes/export.py`
- `daysync/services/api/schemas/models.py`
- `daysync/apps/desktop/src/api/client.ts`
- `daysync/apps/desktop/src/pages/ExportPage.tsx`
- `daysync/apps/desktop/tests/export-page.test.tsx`
- `daysync/packages/daysync_core/tests/test_export.py`
- `daysync/tests/e2e/test_mvp_flow.py`

## 验证结果

以下命令均已通过：

```powershell
uv run pytest packages/daysync_core/tests/test_export.py
pnpm --filter desktop test -- tests/export-page.test.tsx
uv run pytest tests/e2e/test_mvp_flow.py
```

## 当前状态

- 导出链路现已支持：
  - `CSV`
  - `FCP 7 XML`
  - `JSON`
  - `OTIO`
- 最近导出记录可以正确显示不同格式和数量单位。
- 工作区在本次结束时包含新的未提交修改，尚未再次执行备份脚本。

## 下次建议起点

- 如果继续沿导出链路推进，优先考虑：
  - `FCPXML` 导出
  - `OTIO` 结构进一步细化为多 timeline collection，而不是单 timeline 顺排
- 如果偏稳定性，优先考虑：
  - 导出失败态在 UI 上的更明确展示
  - 补更完整的多格式导出页交互测试

## 当前修改位置

- 后端主入口：`daysync/packages/daysync_core/src/daysync_core/export/service.py`
- 前端主入口：`daysync/apps/desktop/src/pages/ExportPage.tsx`
