# DaySync 进度快照

- 时间：2026-05-17 15:26（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 备份提交：`9d312f7` `backup: pre-change snapshot 2026-05-17 14:20:28`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-142028`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/2026-05-17_1424_导出模块五格式收口完成.md`
- 当时虽然功能面已完整，但仍存在真实交付风险：
  - Tauri 壳启动 API 依赖编译时路径和全局 `uv`
  - 换机器后无法保证还能找到正确工作区
  - 缺少正式的 Windows 便携发布目录构建脚本

## 本次完成内容

1. 桌面启动链收口：
   - `src-tauri/main.rs` 改为运行时定位工作区，而不是依赖编译时路径
   - 支持优先查找：
     - 环境变量 `DAYSYNC_WORKSPACE_ROOT`
     - `exe同级/daysync_runtime/`
     - `exe上级祖先目录中的源码工作区`
   - API 拉起优先使用：
     - 工作区 `.venv\Scripts\python.exe`
     - 其次 `uv`
     - 再次 `python`
   - 显式设置 `PYTHONPATH`，避免 editable `.pth` 继续绑死开发机绝对路径
2. 补充 Rust 单测：
   - 便携目录同级 `daysync_runtime` 解析
   - 开发目录祖先工作区解析
   - 本地 `.venv` 启动策略优先级
3. 新增 Windows 便携发布脚本：
   - `daysync/scripts/build_windows_release.ps1`
   - 自动构建 Tauri release
   - 生成 `output/DaySync_Windows_Portable/`
   - 拷贝：
     - `DaySync.exe`
     - `daysync_runtime/.venv`
     - `daysync_runtime/services`
     - `daysync_runtime/packages`
     - `daysync_runtime/tools/ffmpeg/windows-x64/current`
     - `release-manifest.json`
4. 补 README：
   - 增加 Windows 便携发布命令和目录说明
5. 完成真实发布目录构建和烟雾验证：
   - 发布目录内复制后的 Python 已成功导入 `services.api.main`
   - 证明发布目录不依赖开发目录源码路径才能完成基本启动准备

## 这次改动的关键文件

- `daysync/apps/desktop/src-tauri/src/main.rs`
- `daysync/apps/desktop/src-tauri/Cargo.toml`
- `daysync/apps/desktop/src/App.tsx`
- `daysync/scripts/build_windows_release.ps1`
- `daysync/README.md`
- `daysync/.gitignore`

## 验证结果

以下命令均已通过：

```powershell
cargo test
uv run pytest
pnpm --filter desktop test
powershell -ExecutionPolicy Bypass -File .\scripts\build_windows_release.ps1
```

发布目录已生成：

```text
D:\Myproject\DaySync_自动合板软件\daysync\output\DaySync_Windows_Portable\
```

核心可执行文件：

```text
D:\Myproject\DaySync_自动合板软件\daysync\output\DaySync_Windows_Portable\DaySync.exe
```

## 当前状态

- 功能面：
  - MVP 主链完成
  - 自动候选 / 聚类 / 复核完成
  - 导出五格式完成：`CSV / FCP 7 XML / JSON / OTIO / FCPXML`
- 交付面：
  - Tauri release 可编译
  - 便携发布目录可生成
  - 桌面启动链不再依赖编译时源码路径

## 下次建议起点

- 如果继续提升产品完成度，优先考虑：
  - 在另一台干净 Windows 机器上做真实便携包开箱验证
  - 给桌面端补“本地运行时缺失/损坏”的更明确 UI 提示
  - 如需 installer，再基于当前便携目录增加安装器链路

## 当前修改位置

- 桌面启动主入口：`daysync/apps/desktop/src-tauri/src/main.rs`
- 便携发布主入口：`daysync/scripts/build_windows_release.ps1`
