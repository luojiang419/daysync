# DaySync 进度快照

- 时间：2026-05-17 17:21（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 备份提交：`3c07995` `backup: pre-change snapshot 2026-05-17 17:20:55`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-172055`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/10_2026-05-17_1630_旧API替换与mp3外录目录导入收口.md`
- 用户新增目标：
  - 导入视频/音频目录后，直接按目录自动生成整条时间线轨道
  - 不再出现逐个素材复选框
  - 对齐一组字幕后，能够正确覆盖整条平铺轨道，而不是只生成单条同步结果
  - 编译一份新的 Windows 便携版本供测试

## 本次完成内容

1. 目录导入自动整轨：
   - 媒体导入接口在目录导入成功后，会自动重建视频整轨和音频整轨
   - 前端媒体页改成双分栏“视频整轨 / 音频整轨”导入视图
   - 不再要求用户先导入再去时间线页勾选素材手动生成轨道
2. 时间线页改为轨道总览：
   - `FlatTimelinePage` 从“勾选生成器”改成“当前自动整轨总览”
   - 去掉复选框和手动生成入口，直接展示最新视频轨和音频轨的平铺结果
3. 搜索结果按轨道独立返回：
   - 字幕搜索从“全局 limit 后再拆视频/音频”改为“视频轨和音频轨各自独立 limit”
   - 修复了一侧命中多时，把另一侧正确结果挤掉的问题
4. 手动对齐改为整轨批量同步：
   - 选中一组视频/音频字幕锚点后，不再只写入 1 条 `sync_result`
   - 后端会根据整条视频轨和音频轨的平铺关系，批量生成整组同步结果
   - 前端同步提示文案改为整轨对齐成功，并显示本次生成条数
5. 便携包重新编译完成：
   - 使用正式发布脚本生成新的时间戳 Windows 便携目录
   - 默认输出目录未被覆盖

## 关键实证结果

1. 后端与前端验证：
   - `uv run pytest` 全量通过
   - `pnpm --filter desktop test -- tests/media-import-page.test.tsx tests/flat-timeline-page.test.tsx tests/subtitle-match-board.test.tsx` 通过
   - `pnpm --filter desktop build` 通过
2. 搜索逻辑验证：
   - 新增测试覆盖“音频命中过多时，视频结果不会被全局 limit 挤掉”
3. 整轨同步验证：
   - 新增测试覆盖“单次手动锚点对齐后，按整轨批量生成多条同步结果”
4. 新便携包已产出：
   - 发布目录：`daysync/output/DaySync_Windows_Portable_20260517_171050/`
   - 可执行文件：`daysync/output/DaySync_Windows_Portable_20260517_171050/DaySync.exe`
   - `release-manifest.json`、本地 `.venv`、`services/`、项目内 FFmpeg 运行时均已包含

## 当前修改位置

- 自动整轨入口：`daysync/services/api/routes/media.py:22`
- 整轨生成函数：`daysync/packages/daysync_core/src/daysync_core/timeline/service.py:106`
- 搜索分轨 limit：`daysync/packages/daysync_core/src/daysync_core/search/service.py:8`
- 整轨批量同步：`daysync/packages/daysync_core/src/daysync_core/sync/service.py:14`
- 前端媒体整轨页：`daysync/apps/desktop/src/pages/MediaImportPage.tsx:8`
- 前端时间线总览页：`daysync/apps/desktop/src/pages/FlatTimelinePage.tsx:6`
- 前端整轨对齐入口：`daysync/apps/desktop/src/pages/SubtitleSearchAndSyncPage.tsx:429`

## 待办清单（未完成）

1. 剪辑软件导出仍未完全按“整轨连续时间线”收口：
   - 当前 `FCP 7 XML / FCPXML / OTIO` 仍主要按单条 `sync_result` 输出
   - 还没有升级为真正的“整轨连续序列导出”
2. 真实项目实机链路还需要继续验证：
   - 导入真实视频/音频目录
   - 导入第三方整轨识别字幕
   - 执行整轨一键对齐
   - 导出并回灌到剪辑软件验证
3. 进度快照规范刚刚补齐为编号模式：
   - 后续新快照应直接从 `12_...` 继续递增

## 下一步建议

1. 先用真实工程跑一遍完整链路：
   - 媒体目录导入
   - 自动整轨
   - 字幕导入
   - 整轨一键对齐
   - 查看导出条数是否和整轨切片预期一致
2. 如果下一步是剪辑软件交付，优先继续收口：
   - 把 `FCP 7 XML / FCPXML / OTIO` 改成真正的整轨连续导出
   - 再做 Premiere / 剪映 / 其他目标剪辑软件的实机导入验证
