# DaySync 进度快照

- 时间：2026-05-17 19:54（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 备份提交：`5c8b526` `backup: pre-change snapshot 2026-05-17 19:34:52`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-193452`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/11_2026-05-17_1721_目录整轨同步与新便携包编译收口.md`
- 用户新增目标：
  - `/timeline` 页面不再是文本列表，而是接近剪辑软件布局的剪辑台
  - 需要集成平铺时间线展示、字幕加载、视频预览、字幕叠加显示、视频/音频文件名叠加显示
  - 保留当前 `/subtitles` 作为高级同步工具页，不把整站全部推翻

## 本次完成内容

1. 新增 studio 聚合读模型接口：
   - 新增 `GET /api/projects/{project_id}/studio/timeline`
   - 一次性返回：
     - 最新视频整轨
     - 最新音频整轨
     - 最新视频字幕轨
     - 最新音频字幕轨
     - `video_clips`
     - `audio_clips`
     - `video_subtitles`
     - `audio_subtitles`
     - `sync_segments`
     - `accepted_sync_summary`
2. 替换 `/timeline` 页面为剪辑台：
   - 左侧：整轨摘要、字幕加载、快速搜索、当前已选锚点
   - 中间：播放器和字幕/文件名叠加
   - 右侧：当前检查器
   - 底部：四轨时间线
     - `V1` 视频素材轨
     - `S1` 视频字幕轨
     - `A1` 外录音频轨
     - `S2` 音频字幕轨
3. 时间线交互改造：
   - 片段和字幕都改成横向 block
   - 轨道顶部带时间尺
   - 支持点击片段跳转
   - 支持点击字幕块选中锚点
   - 支持播放头移动和当前命中高亮
   - 支持缩小 / 适配 / 放大三种只读预览缩放
4. 本地媒体预览接通：
   - Tauri 侧开启 `protocol-asset`
   - `tauri.conf.json` 开启 `assetProtocol`
   - 前端通过 `convertFileSrc(original_path)` 生成可播放本地资源 URL
   - 有 accepted 同步结果时，播放器按 `sync_segments` 联动外录音频
   - 没有 accepted 同步结果时，回退为视频参考音预览
5. 测试补齐：
   - 新增后端 `test_studio.py`
   - 前端 `flat-timeline-page.test.tsx` 改为验证剪辑台布局和回退提示

## 关键实证结果

1. 后端全量测试通过：
   - `uv run pytest`
2. 前端全量测试通过：
   - `pnpm --filter desktop test`
3. 前端构建通过：
   - `pnpm --filter desktop build`
4. Tauri 桌面正式构建通过：
   - `pnpm --filter desktop tauri build`
5. `protocol-asset` 依赖已接通：
   - `daysync/apps/desktop/src-tauri/Cargo.toml` 增加 `tauri = { version = "2", features = ["protocol-asset"] }`
   - `Cargo.lock` 同步引入 `http-range`

## 当前修改位置

- studio 聚合服务：`daysync/packages/daysync_core/src/daysync_core/studio/service.py:7`
- studio API 路由：`daysync/services/api/routes/studio.py:12`
- API 主入口挂载：`daysync/services/api/main.py:15`
- Tauri 媒体 URL 工具：`daysync/apps/desktop/src/api/tauri.ts:61`
- 前端 studio 类型：`daysync/apps/desktop/src/api/types.ts`
- `/timeline` 剪辑台页：`daysync/apps/desktop/src/pages/FlatTimelinePage.tsx:32`
- 播放器组件：`daysync/apps/desktop/src/components/studio/StudioPreviewPlayer.tsx:21`
- 四轨时间线组件：`daysync/apps/desktop/src/components/studio/StudioTimelineCanvas.tsx:24`
- 资产协议配置：`daysync/apps/desktop/src-tauri/tauri.conf.json:15`

## 待办清单（未完成）

1. 当前剪辑台还是“只读预览版”：
   - 不支持拖拽剪辑
   - 不支持裁切
   - 不支持重排
   - 不支持波纹编辑
2. 当前播放器联动是 v1 非无缝切源方案：
   - 跨片段时允许重新装载视频/音频源
   - 已满足正确性验证，但还不是专业剪辑软件级无缝体验
3. `/subtitles` 高级工具页还没有和新剪辑台做更深的状态共享：
   - 当前是“并行存在，可分别工作”
4. 导出链路仍未升级为整轨连续序列导出：
   - `FCP 7 XML / FCPXML / OTIO` 还需要继续收口

## 下一步建议

1. 先用真实项目做一次桌面实机验证：
   - 导入视频/音频目录
   - 加载 `video_flat.srt` / `audio_flat.srt`
   - 用快速搜索或字幕轨直接选锚点
   - 执行整轨一键对齐
   - 在剪辑台中播放并确认字幕叠加、文件名叠加、外录音频联动都正确
2. 如果下一步继续做交付闭环，优先推进：
   - 整轨连续导出
   - 剪辑台与 `/subtitles` 页的更深联动
   - 更平滑的跨片段预览体验
