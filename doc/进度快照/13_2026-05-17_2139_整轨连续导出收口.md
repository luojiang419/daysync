# DaySync 进度快照

- 时间：2026-05-17 21:39（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 备份提交：`ef7e773` `backup: pre-change snapshot 2026-05-17 19:56:26`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-195626`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/12_2026-05-17_1954_剪辑台式时间线与本地媒体预览收口.md`
- 继续收口目标：
  - 把 `FCP 7 XML / FCPXML / OTIO` 从“按每条 sync_result 导出”升级为“按整轨连续片段导出”
  - 让剪辑软件拿到的是一条连续序列，而不是多条离散项目/sequence

## 本次完成内容

1. 导出数据源改造：
   - 新增连续导出片段装载函数：`_load_continuous_export_segments()`
   - 不再直接拿 `_load_rich_sync_export_rows()` 做连续序列导出
   - 连续导出片段现在来自 `studio` 聚合快照里的 `sync_segments`
   - 同时补齐媒体元数据和同步元数据：
     - `confidence_score`
     - `status`
     - `source`
     - `video_anchor_text`
     - `audio_anchor_text`
2. `FCP 7 XML` 收口为单一连续 sequence：
   - 从“每条同步结果一个 sequence”
   - 改成“整个项目一个连续 sequence”
   - 视频轨和音频轨的 clip 按整轨时间位置排布
3. `FCPXML` 收口为单一连续 project：
   - 从“每条同步结果一个 project”
   - 改成“整个项目一个 project”
   - spine 内按整轨顺序排列视频片段，嵌套外录音频片段
4. `OTIO` 收口为整轨连续 timeline：
   - 统一按整轨时间位置构造 `V1 / A1`
   - 自动插入 gap
   - 不再按单条结果各自生成独立局部结构
5. 测试同步升级：
   - `test_export.py` 新增多片段整轨连续导出断言
   - `test_mvp_flow.py` 更新端到端导出计数期望

## 关键实证结果

1. 导出定向测试通过：
   - `uv run pytest packages/daysync_core/tests/test_export.py tests/e2e/test_mvp_flow.py`
2. 后端全量测试通过：
   - `uv run pytest`
3. 前端全量测试通过：
   - `pnpm --filter desktop test`
4. 前端构建通过：
   - `pnpm --filter desktop build`

## 当前修改位置

- 连续导出片段装载：`daysync/packages/daysync_core/src/daysync_core/export/service.py:305`
- `FCPXML` 连续 project 导出：`daysync/packages/daysync_core/src/daysync_core/export/service.py:381`
- `OTIO` 连续 timeline 导出：`daysync/packages/daysync_core/src/daysync_core/export/service.py:537`
- `FCP 7 XML` 连续 sequence 导出：`daysync/packages/daysync_core/src/daysync_core/export/service.py:591`
- 多片段导出测试：`daysync/packages/daysync_core/tests/test_export.py:145`
- e2e 计数回归：`daysync/tests/e2e/test_mvp_flow.py`

## 待办清单（未完成）

1. 剪辑台和导出链路已接通，但还缺真实工程实机验证：
   - 用真实目录导入
   - 真实字幕加载
   - 整轨一键对齐
   - 导出后回灌剪辑软件验证
2. 当前剪辑台仍是只读预览版：
   - 不支持拖拽编辑
   - 不支持裁切
   - 不支持重排
3. 播放器跨片段切换仍是 v1 非无缝方案
4. `/subtitles` 高级工具页与剪辑台之间仍缺更深的状态共享

## 下一步建议

1. 优先做真实项目桌面实机验证：
   - 验证剪辑台预览
   - 验证字幕叠加
   - 验证整轨连续导出能否被目标剪辑软件正确识别
2. 如果实机验证通过，再继续做：
   - 播放器跨片段平滑切换
   - 剪辑台与 `/subtitles` 页共享更多状态
   - 更接近专业剪辑软件的编辑交互
