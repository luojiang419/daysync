# DaySync 进度快照

- 时间：2026-05-17 22:39（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 源码压缩备份：`backup/backup_v002_20260517_222432.zip`
  - 备份提交：`9c17b5a` `backup: pre-change snapshot 2026-05-17 22:21:38`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-222138`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/14_2026-05-17_2200_本地API自动拉起与重试收口.md`
- 用户新要求：
  - 不再接受“启动时偶发连不上本地 API”
  - 需要直接按方案执行：全仓移除 FastAPI，改为 Tauri 直连本地 Python bridge

## 本次完成内容

1. Python bridge 已落地：
   - 新增 `daysync_core.bridge`，把原来 `services/api/routes` 的 23 个方法语义统一收进 dispatcher
   - 新增持久化 worker，采用 `stdin/stdout` JSON 行协议
   - worker 会话状态接管原 `AppState` 的项目注册和 FFmpeg 状态缓存
2. 桌面端运行时已改造：
   - `src-tauri` 不再启动 `uvicorn`
   - 改为启动 `python -m daysync_core.bridge.worker`
   - 新增 `ensure_runtime_ready` 与 `invoke_runtime`
   - 内置单例 worker 管理与一次自动重启兜底
3. 前端通信层已切换：
   - `apps/desktop/src/api/client.ts` 已从 `fetch` 改为 Tauri invoke
   - 页面层方法签名保持不变
   - 启动页、项目页、侧边栏状态文案已全部改成“本地运行时”
4. FastAPI 残留已删除：
   - 已删除 `daysync/services/api/`
   - 已删除 `daysync/tests/test_api_cors.py`
   - `pyproject.toml` / `uv.lock` 已去掉 FastAPI、uvicorn、pydantic 残留依赖
5. 发布链已收口：
   - `README.md` 改为本地 Python bridge 运行时说明
   - `scripts/build_windows_release.ps1` 不再复制 `services/`
   - 便携包 smoke check 改为检查 `RuntimeDispatcher`

## 关键实证结果

1. Python 端到端 bridge 验证通过：
   - `uv run pytest tests/e2e/test_mvp_flow.py`
2. Python 全量测试通过：
   - `uv run pytest`
3. 前端全量测试通过：
   - `pnpm --filter desktop test`
4. 前端生产构建通过：
   - `pnpm --filter desktop build`
5. Tauri Rust 单测通过：
   - `cargo test`
6. Windows 便携包构建通过：
   - `pwsh -File scripts/build_windows_release.ps1 -OutputRoot daysync/output/DaySync_Windows_Portable_20260517_223646`
7. 新便携包关键产物存在：
   - `daysync/output/DaySync_Windows_Portable_20260517_223646/DaySync.exe`
   - `daysync/output/DaySync_Windows_Portable_20260517_223646/release-manifest.json`
   - `daysync/output/DaySync_Windows_Portable_20260517_223646/daysync_runtime/`

## 当前修改位置

- Python bridge dispatcher：`daysync/packages/daysync_core/src/daysync_core/bridge/dispatcher.py:58`
- Python bridge worker：`daysync/packages/daysync_core/src/daysync_core/bridge/worker.py:9`
- Tauri 运行时命令：`daysync/apps/desktop/src-tauri/src/main.rs:234`
- Tauri 调用入口：`daysync/apps/desktop/src-tauri/src/main.rs:239`
- 前端运行时包装：`daysync/apps/desktop/src/api/tauri.ts:28`
- 前端 client 主入口：`daysync/apps/desktop/src/api/client.ts:148`
- 启动页运行时接入：`daysync/apps/desktop/src/App.tsx:28`
- 项目页运行时健康状态：`daysync/apps/desktop/src/pages/ProjectHomePage.tsx:26`
- 侧边栏运行时状态展示：`daysync/apps/desktop/src/components/AppShell.tsx:40`
- e2e bridge 验证：`daysync/tests/e2e/test_mvp_flow.py:10`
- 便携包 smoke import：`daysync/scripts/build_windows_release.ps1:75`

## 当前产物

- 最新便携包目录：`daysync/output/DaySync_Windows_Portable_20260517_223646/`
- 最新可执行文件：`daysync/output/DaySync_Windows_Portable_20260517_223646/DaySync.exe`
- 最新运行时根目录：`daysync/output/DaySync_Windows_Portable_20260517_223646/daysync_runtime/`

## 待办清单（未完成）

1. 还需要真实桌面冷启动实机验证：
   - 直接双击 `DaySync.exe`
   - 不手动启动任何 Python / API
   - 验证创建项目、打开项目、导入、时间线、字幕、导出
2. 还需要确认冷启动后系统里不再监听 `127.0.0.1:17831`
3. 之前剪辑台 v1 的交互限制仍然存在：
   - 跨片段切换仍非无缝
   - 剪辑台仍偏只读预览

## 下一步建议

1. 优先在真实 Windows 桌面环境跑新便携包，重点看：
   - 是否再也不会出现“未连接到本地 API”
   - 是否完全不依赖 17831 端口
   - 便携包自带 `.venv + bridge + ffmpeg` 是否稳定
2. 如果这轮实机验证通过，再继续推进：
   - 剪辑台交互增强
   - 更平滑的播放器跨片段体验
   - `/subtitles` 与剪辑台状态共享加深
