# DaySync 进度快照

- 时间：2026-05-18 09:35（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 备份提交：`a210c04` `backup: pre-change snapshot 2026-05-18 09:17:54`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260518-091754`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/17_2026-05-18_0858_当前合板流程图与目标流程图梳理.md`
- 用户本轮要求：
  - 按既定方案直接实现“自动整日合板”主链
  - 目标包括：字幕反解、自动上下文找锚点、整轨 offset 聚类、自动裁切生成整日时间线、导出 Premiere / DaVinci XML

## 本次完成内容

1. 后端自动整日合板 runtime 已落地：
   - 新增 `sync.auto_conform_preview`
   - 新增 `sync.apply_auto_conform`
   - 预览阶段只分析不落库，应用阶段才生成整日 `sync_results`
2. 字幕反解素材级视图已接入 `studio.timeline`：
   - 新增 `video_source_subtitle_groups`
   - 新增 `audio_source_subtitle_groups`
   - 新增 `auto_conform_readiness`
3. 自动整日合板规则已接入：
   - 视频字幕按原始素材分组
   - 每个素材最多选 2 条种子
   - 排除 `mapping != ok`、空文本、短文本、高重复文本
   - 多句上下文匹配音频候选
   - track offset 聚类后生成整日提案
4. 自动整日合板应用链已接通：
   - 高置信写入 `accepted_auto + source=auto_text`
   - 低置信需要人工确认后写入 `accepted_manual + source=auto_text`
   - 应用前会清理旧的 `auto_text` 整日结果
5. 导出语义已收口：
   - `export.fcp7_xml` 对应 Premiere XML
   - `export.fcpxml` 对应 DaVinci XML
   - 若没有 accepted 整轨结果，XML 导出会直接报错而不是输出空时间线
6. 前端主工作台已升级：
   - `FlatTimelinePage` 新增“字幕反解概览”
   - 新增“自动整日合板”预览卡片
   - 自动提案可显示代表锚点、候选锚点组、排除原因、推荐 offset、预览片段数
   - 保留“手动兜底”支线
7. 导出页文案已同步：
   - 按钮改为“导出 Premiere XML / 导出 DaVinci XML”
   - 导出历史显示也同步为这两个交付名称
8. 流程图文档已同步到新主链：
   - `doc/DaySync_合板流程图.md` 已改成“自动整日合板主链 + 手动兜底支线”

## 关键修改位置

- 自动整日合板预览：`daysync/packages/daysync_core/src/daysync_core/sync/service.py:80`
- 自动整日合板应用：`daysync/packages/daysync_core/src/daysync_core/sync/service.py:199`
- 视频种子筛选：`daysync/packages/daysync_core/src/daysync_core/sync/service.py:1012`
- studio 素材级字幕分组：`daysync/packages/daysync_core/src/daysync_core/studio/service.py:41`
- studio 自动合板就绪摘要：`daysync/packages/daysync_core/src/daysync_core/studio/service.py:222`
- XML 导出前校验：`daysync/packages/daysync_core/src/daysync_core/export/service.py:301`
- runtime 方法注册：`daysync/packages/daysync_core/src/daysync_core/bridge/dispatcher.py:73`
- 前端自动整日合板按钮与预览：`daysync/apps/desktop/src/pages/FlatTimelinePage.tsx:363`
- 前端字幕反解概览与 readiness 展示：`daysync/apps/desktop/src/pages/FlatTimelinePage.tsx:561`
- 导出页 Premiere / DaVinci 文案：`daysync/apps/desktop/src/pages/ExportPage.tsx:455`

## 关键实证结果

1. Python 自动整日合板测试通过：
   - `uv run pytest tests/test_auto_conform_flow.py`
2. Python 既有端到端流程测试通过：
   - `uv run pytest tests/e2e/test_mvp_flow.py`
   - `uv run pytest tests/test_bridge_worker_utf8_io.py`
3. 桌面前端全量测试通过：
   - `pnpm --filter desktop test`
4. 桌面前端生产构建通过：
   - `pnpm --filter desktop build`
5. Tauri Rust 单测通过：
   - `cargo test`
6. 新便携包构建通过：
   - `pwsh -File daysync/scripts/build_windows_release.ps1 -OutputRoot daysync/output/DaySync_Windows_Portable_20260518_0934`

## 当前产物

- 最新便携包目录：`daysync/output/DaySync_Windows_Portable_20260518_0934/`
- 最新可执行文件：`daysync/output/DaySync_Windows_Portable_20260518_0934/DaySync.exe`
- 最新运行时根目录：`daysync/output/DaySync_Windows_Portable_20260518_0934/daysync_runtime/`
- 最新自动整日合板回归测试：`daysync/tests/test_auto_conform_flow.py`

## 待办清单（未完成）

1. 还没有把媒体导入页升级成“视频目录 / 音频目录”双分栏主入口
2. 还没有把自动建轨接到目录导入后的默认流程
3. 自动整日合板 preview 还偏工程型信息展示，后续可继续做得更像候选审查面板
4. 当前仍未引入实体 WAV 裁切，这一轮固定采用 XML 内 `in/out` 裁切

## 下一步建议

1. 直接以 `FlatTimelinePage` 作为新的自动整日合板主工作台继续打磨
2. 下一轮优先做：
   - 媒体导入页双分栏目录导入
   - 导入后自动建轨
   - 时间线视觉继续靠近剪辑软件风格
3. 如果你要验证交付链，优先检查：
   - Premiere XML 是否满足你的 PR 导入习惯
   - DaVinci XML 是否满足 Resolve 导入习惯
