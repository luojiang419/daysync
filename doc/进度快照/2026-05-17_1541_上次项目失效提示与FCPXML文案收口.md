# DaySync 进度快照

- 时间：2026-05-17 15:41（Asia/Shanghai）
- 当前分支：`main`
- 本轮开始前备份状态：
  - 备份提交：`cbbf7a0` `backup: pre-change snapshot 2026-05-17 15:31:48`
  - 远端 `origin/main` 已推送成功
  - 远端备份 tag 已推送成功：`backup/20260517-153148`

## 本次续写起点

- 已读取上一份快照：`doc/进度快照/2026-05-17_1534_API连接与上次项目自动恢复收口.md`
- 上一轮完成后仍剩下两个明显的收口点：
  - `FCPXML` 已实现，但导出页页头说明漏写
  - “上次项目自动恢复”在目录失效时虽然会清理本地记录，但缺少明确 UI 提示

## 本次完成内容

1. 补“上次项目目录失效”提示：
   - App 启动时如果自动恢复失败，会：
     - 清理 `daysync.last_project_root`
     - 弹出明确提示：
       - `上次项目目录已失效，自动恢复记录已清除，请重新打开项目。`
2. 补 `FCPXML` 导出页说明：
   - 导出页页头从
     - `CSV / FCP 7 XML / JSON / OTIO`
   - 改为
     - `CSV / FCP 7 XML / JSON / OTIO / FCPXML`
3. 补对应前端测试：
   - 自动恢复失败时的本地存储清理与提示
   - 导出页页头包含 `FCPXML`

## 这次改动的关键文件

- `daysync/apps/desktop/src/App.tsx`
- `daysync/apps/desktop/src/pages/ExportPage.tsx`
- `daysync/apps/desktop/tests/app-bootstrap.test.tsx`
- `daysync/apps/desktop/tests/export-page.test.tsx`

## 验证结果

以下命令均已通过：

```powershell
pnpm --filter desktop test -- tests/app-bootstrap.test.tsx tests/export-page.test.tsx
pnpm --filter desktop test
pnpm --filter desktop build
```

## 当前状态

- 上次项目自动恢复链现在不仅能恢复，也能在失效时明确告知用户。
- 导出模块页头说明与真实功能现在一致，已包含 `FCPXML`。
- 这一轮属于明显未收口细节的继续补齐，不涉及新的功能扩展。

## 下次建议起点

- 如果继续做“未完成功能”，下一步更适合进入这些范围：
  - `FCPXML` 真实剪辑软件导入验证
  - 便携版 `DaySync.exe` 的完整开箱恢复链路实测
  - 更完整的“上次项目失效”交互，例如提供一键重新选择目录

## 当前修改位置

- 前端启动入口：`daysync/apps/desktop/src/App.tsx`
- 导出页入口：`daysync/apps/desktop/src/pages/ExportPage.tsx`
